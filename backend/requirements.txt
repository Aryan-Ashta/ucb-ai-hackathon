# Pinned to versions verified against the test suite on 2026-06-21.
# Bump deliberately — `pip install -r requirements.txt` after a change
# should be a conscious decision, not a silent drift from upstream.
#
# P2-B6: closes the long-standing "9 unpinned deps" finding from STATUS.md.

# --- runtime dependencies ---
fastapi==0.138.0
uvicorn==0.49.0
python-dotenv==1.2.2
httpx==0.28.1
openai==2.43.0
redis==8.0.0
sentry-sdk==2.63.0
pydantic==2.13.4
python-multipart==0.0.20
cachetools==7.1.4
cryptography==49.0.0

# --- test dependencies ---
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
fakeredis==2.36.2
